You are a DESIGN SPIKE agent on The Observatory (prd3 — read docs/prd3.md
first, fully). Your branch is DISPOSABLE: it will never merge. You are one
of three directions built for a side-by-side taste ruling. Build real,
running code against the real event stream — beauty judged in a browser,
not in prose.

DIRECTION C — MYCELIUM PULSE-NETWORK. The direction that makes FLOW
visible — the thing no other rendering shows. Main is a root-mass; lanes
are nodes it threads to; and along those luminous threads travel PULSES:
a commit is a packet of light running home, tokens are a drift of faint
motes, a landing is a bright surge into the root-mass. The still image is
quiet and near-monochrome-dark; the LIFE is in what moves along the
threads. Threads thin, bundle and branch like hyphae — subagent threads
split off their worker's thread as finer filaments (thread data exists —
use it).

BUILD (one page, real data): the attention strip (ALL CLEAR ↔ "N need
attention" with named items), the fleet table (dense, mono-tabular), the
burn strip (output, $, rate, ratio), and the scene as described.

THE SCENE CONTRACT (non-negotiable, encodings are yours): a needs-you/stuck
lane is the single most salient object; work size (output tokens) readable;
recency readable; ALL FIVE pathologies distinguishable — LOOPING is a pulse
orbiting a visible knot, going nowhere; FROZEN is a thread gone dark;
WAITING is a held pulse at the node, pulsing in place like a raised hand
(must NOT resemble frozen); EXPENSIVE is a thread running white-hot;
OFF-FENCE is a rogue filament crossing into another lane's territory;
learnable in under 30 seconds with no text legend.

CAUTION (motion law tension, handle deliberately): pulses are continuous by
nature but motion is event-currency — every pulse must BE an event (a real
commit, a real usage record), never decorative particles. If nothing is
happening, the network is still. Respect prefers-reduced-motion (pulses
become static brightness gradients).

LAWS: status hues exclusive to the ladder (neutral/cyan/amber/magenta-red);
sans labels, mono-tabular data; render everything always (operator ruling)
— all lanes threaded at any count.

FIXTURES: (1) live stream, (2) 20-lane synthetic fixture with subagent
filaments, (3) staged-pathology fixture (one of each pathology). Keyboard
toggle 1/2/3.

DELIVERABLES: running page; screenshots of all three fixtures under
spike-artifacts/; spike-artifacts/NOTES.md naming your encoding choices,
how pulse-as-event held up against the motion law, and what you'd
steal-proof.

RULES: packages/web is yours to reshape freely (disposable branch); do not
touch packages/core or packages/server except read-only imports; commit
your work (REQUIRED); never push, merge, or run git in a sibling worktree;
no NUL bytes; STOP when the three fixtures render and the notes are
committed.
