You are a worker agent on rhizomorph. You own exactly one issue.
Read every document your issue names IN FULL before changing
anything; import from @rhizomorph/core; laws restated stronger,
never weakened. Tests HERMETIC under 4x concurrency.

YOUR ISSUE — #161:

## Direction

Operator ruling after reviewing the replay (prd10 rulings 13–16, read
them IN FULL first — they knowingly rescind prd5's cord-cut and the
deletion half of prd10 ruling 2). The rhizomorph is a root-cord network;
a mycelial network does not delete the cords that carried its nutrients.
**Completed strands stay.**

1. **Persist the strand.** A landed/severed lane's thread is no longer
   removed from the scene. Delete the retirement path that erases
   geometry (`retire.ts` and whatever the dissolve completion calls) and
   replace it with a transition to a PERSISTENT state.
2. **Luminous, not alive** (ruling 14): the persistent strand is thin,
   still, and lit — light without animation, without glow, without any
   alarm-grammar instrument, below the calm ceiling. Living strands must
   stay unmistakably dominant at a glance: thicker, brighter, moving.
   Name the constants (`PERSIST_WIDTH_SCALE`, `PERSIST_LUMINANCE`, …) at
   the top of the file with their law comments.
3. **Completion is a transformation, not a deletion** (ruling 15): the
   return beat survives EXACTLY as built — motes lift from the cord and
   stream home, the heart takes its ring — and at the end the strand
   remains, thinned and stilled. What travels home is the lane's
   vitality, not its existence. The motes still wear family hue cooling
   through the tissue accent (ruling 12).
4. **Density by hierarchy** (ruling 16): with dozens of finished lanes
   the field must still read. Buy it with thinness, stillness and depth
   layering (persistent strands sit behind living ones), never by
   removing history. The existing HIDE FINISHED control becomes
   load-bearing — make sure it still works and reads as obvious.
5. **Measure** (the operator is chasing stutter elsewhere): report
   before/after frame numbers with 30 lanes where most are persistent —
   use the interleaved method the perf test already documents, and do not
   let persistent strands cost more than living ones did.

## Fence (may touch ONLY)

- `packages/web/src/scene/` (all files)

## Blocked by

Nothing (#157 landed). **Model:** opus (taste + a law reversal).
**Wave:** the persistent network.

## Definition of done

- Completed strands persist, thin/still/luminous, behind living ones;
  the return beat (motes + ring) is unchanged; no strand geometry is
  ever deleted.
- Living-vs-finished hierarchy provable at a glance and test-stated
  (a finished strand's width/luminance is strictly below a living one's).
- Scene laws + the 9b amendment green; before/after frames reported.
- Root `npm test` + `npm run typecheck` green.
- Say what you would show the operator first.

RULES: stay strictly inside the FENCE; small conventional commits
(committing is REQUIRED); NEVER switch branches, push, merge, or run
git in a sibling worktree; no NUL bytes; tests deterministic; build
for a stranger's machine; if you cannot proceed print
"BLOCKED: <need>" and stop; DoD is root 'npm test' +
'npm run typecheck' green, then STOP with a short summary.
