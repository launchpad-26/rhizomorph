You are a worker agent on The Rhizomorph (prd2: anyone, anywhere).
You own exactly one issue.

FIRST read docs/prd2.md — it explains why this work exists — then the
files your issue names. The acceptance test for this whole prd is that a
stranger on a fresh machine can run the app from the README alone, so
prefer being explicit and loud over being clever.

YOUR ISSUE — #55 (55. CI: clean clone, install, build, test, boot smoke)

**Fence (may touch ONLY):** `.github/workflows/ci.yml` (new)
**Blocked by:** #51. **Model:** sonnet. **Wave: D**

There is no CI. Nothing has ever verified that a fresh clone works — which is
exactly why the broken run command (#51) survived to ship.

Add a GitHub Actions workflow that does what a stranger does:
- checkout clean, Node from `engines`, `npm ci` (not `npm install` — it must
  prove the lockfile is honest);
- `npm run build`;
- `npm test` and `npm run typecheck`;
- **boot smoke**: start the server on a free port against the checkout itself,
  poll `/api/meta` until it answers or a timeout fails the job, confirm `/`
  returns HTML (not a JSON 404), then shut down cleanly.

Keep it one job, no matrix, no caching cleverness. It must fail loudly if the
documented commands stop working.

The root `package.json` is NOT in your fence: every script this workflow needs
already exists (`build`, `test`, `typecheck`, `start` — landed with #51). Do
the boot smoke inline in the workflow. If a script you need is genuinely
missing, do NOT edit outside the fence — stop and report `BLOCKED: <need>`.

**DoD:** root `npm test` + `npm run typecheck` green; deterministic tests (no waitFor racing an async boundary); no NUL bytes. Never push, merge, or run git in a sibling worktree — committing on YOUR branch is required. Finish with a short summary including any live evidence the issue asks for. Additionally: state whether the workflow ran green (push a branch or
explain how you validated it locally, e.g. by executing the same steps in a
clean clone).


RULES: stay strictly inside the FENCE (other agents work in parallel);
import from @rhizomorph/core, never redefine its types; small
conventional commits; committing on YOUR branch is REQUIRED; never push,
merge, or run git in a sibling worktree; no NUL bytes; STOP when done.
