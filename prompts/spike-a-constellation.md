You are a DESIGN SPIKE agent on The Rhizomorph (prd3 — read docs/prd3.md
first, fully). Your branch is DISPOSABLE: it will never merge. You are one
of three directions built for a side-by-side taste ruling. Build real,
running code against the real event stream — beauty judged in a browser,
not in prose.

DIRECTION A — CONSTELLATION REFINED.
Today's scene, perfected under the new laws. Beads for lanes, but every
pixel disciplined: status hues reserved for the ladder (calm neutral /
notice cyan / needs-you amber / broken magenta-red), identity by lightness
and glyph, dense mono-tabular data below. You are the baseline the wilder
two directions must beat — make "we were already right" as strong as it can
possibly be. The refinement IS the design statement: perfect spacing,
perfect contrast spend, one accent family, motion only on events (a landing
streaks once; an attention pulse fires once then holds steady amber).

BUILD (one page, real data): the attention strip (ALL CLEAR ↔ "N need
attention" with named items), the fleet table (dense, 10+ lanes visible,
mono-tabular numerals), the burn strip (output tokens, $ if authoritative,
burn rate, overhead ratio — four numbers, no chrome), and the scene.

THE SCENE CONTRACT (non-negotiable, encodings are yours): a needs-you/stuck
lane is the single most salient object on screen; relative work size
(output tokens) readable; recency readable; ALL FIVE pathologies
distinguishable at a glance — LOOPING (visible cycle), FROZEN (dead air),
WAITING (raised hand — must NOT resemble frozen), EXPENSIVE (heat),
OFF-FENCE (trespass); learnable in under 30 seconds with no text legend.

FIXTURES: render three states — (1) the live stream as-is, (2) a 20-lane
synthetic fixture you build, (3) a staged-pathology fixture with exactly one
looping, one frozen, one waiting, one expensive, one off-fence lane. Wire a
keyboard toggle (1/2/3) between them.

DELIVERABLES: running page on your worktree's dev server; screenshots of
all three fixtures committed to your branch under spike-artifacts/; a
SHORT design note (one file, spike-artifacts/NOTES.md) naming your
encoding choices and what you'd steal-proof about them.

RULES: packages/web is yours to reshape freely (disposable branch); do not
touch packages/core or packages/server except read-only imports; commit
your work (REQUIRED — review happens from your branch); never push, merge,
or run git in a sibling worktree; no NUL bytes; STOP when the three
fixtures render and the notes are committed.
