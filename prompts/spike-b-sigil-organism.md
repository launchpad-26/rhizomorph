You are a DESIGN SPIKE agent on The Rhizomorph (prd3 — read docs/prd3.md
first, fully). Your branch is DISPOSABLE: it will never merge. You are one
of three directions built for a side-by-side taste ruling. Build real,
running code against the real event stream — beauty judged in a browser,
not in prose.

DIRECTION B — THE SIGIL ORGANISM. The operator's own concept; make it sing.
An Obsidian-graph-like, floaty, force-directed field. At its center: the
sigil-core — main/the repo as a breathing glyph-heart. From it, TENDRILS of
cyber-sigilist linework — sharp tapered strokes, thorn-curl terminals,
marks that read as language — reach out to lane-nodes. The organism is felt,
never identified: NO literal creature, no face, no anatomy. Pure neon glow
on near-black, but with an artist's hand: every stroke deliberate, every
glyph meaningful. Lanes carry sigil marks that ARE their state glyphs.
Workers that deploy subagents sprout SECOND-GENERATION filaments to smaller
nodes (thread data exists — use it): the organism has generations.

OPERATOR RULING you must honor: render EVERYTHING, always — every lane
tendriled, every subagent filamented, at any count. Trust force physics and
your design skill to keep it legible; the 20-lane fixture is where this
ruling gets tested.

BUILD (one page, real data): the attention strip (ALL CLEAR ↔ "N need
attention" with named items), the fleet table (dense, mono-tabular), the
burn strip (output, $, rate, ratio), and the scene as described.

THE SCENE CONTRACT (non-negotiable, encodings are yours): a needs-you/stuck
lane is the single most salient object; work size (output tokens) readable
(node/limb weight); recency readable; ALL FIVE pathologies distinguishable
as limb-and-glyph behavior — LOOPING coils in a visible cycle, FROZEN
stiffens and greys, WAITING raises its tip (a hand — must NOT resemble
frozen), EXPENSIVE runs hot, OFF-FENCE visibly trespasses toward another
lane's territory; learnable in under 30 seconds with no text legend.

LAWS: status hues exclusive to the ladder (neutral/cyan/amber/magenta-red);
motion on events only, ONE ambient exception — the core's low-amplitude
breathing; sans labels, mono-tabular data; prefers-reduced-motion respected.

FIXTURES: (1) live stream, (2) 20-lane synthetic fixture WITH subagent
generations, (3) staged-pathology fixture (one of each pathology).
Keyboard toggle 1/2/3.

DELIVERABLES: running page; screenshots of all three fixtures under
spike-artifacts/; spike-artifacts/NOTES.md naming your encoding choices,
how the render-everything ruling held up at 20 lanes, and what you'd
steal-proof.

RULES: packages/web is yours to reshape freely (disposable branch); do not
touch packages/core or packages/server except read-only imports; commit
your work (REQUIRED); never push, merge, or run git in a sibling worktree;
no NUL bytes; STOP when the three fixtures render and the notes are
committed.
