## Direction (prd6 wave 2 — document the living cycle)

- **README.md + docs/demo.md**: the scene's channels retold for a
  first-time reader now that two changed — seed size = work done
  (absolute, capped), **distance = lifecycle journey** (born at the
  centre, retiring at the rim), angle = identity, brightness = recency.
  The cycle: a landing sends its substance home into main, the root-mass
  thickens, a dormant seed rests at the rim, and a returning lane
  germinates from it. Clicking MAIN opens the conductor's own
  conversation.
- **docs/architecture.md decision log**: prd6 rulings 1–5, each with its
  why — absolute-vs-relative sizing (the whale problem), recency losing
  the distance channel (it failed the layman bar), translocation as the
  honest reading of a merge, slot reservation for germination, and the
  MAIN pseudo-lane's deliberate NON-membership in the fleet object. Note
  explicitly that ruling 1 overrules #102's uniform-scar decision and
  that ruling 4 supersedes prd3 graft g6.
- **docs/screenshots/**: regenerate from the live app. Catch, if you
  can: a scene with seeds of visibly different sizes, a rim of scars,
  and the MAIN drawer open on the conductor's conversation.
- Every command verified by running it (say which); no personal paths;
  ruling numbers cited.

## Fence (may touch ONLY)

- `README.md`
- `docs/demo.md`, `docs/architecture.md`
- `docs/screenshots/**`

## Blocked by

#106, #107 (documents what landed). **Model:** sonnet. **Wave:** 2.

## Definition of done

- Root `npm test` + `npm run typecheck` green (docs-only; prove the tree
  unbroken).
- Screenshots regenerated from the live app and committed.

## RULES

- Work ONLY in this worktree. Never run git in any other worktree or the
  main checkout.
- **Committing your work is REQUIRED.** Never push, never merge.
- Build for a stranger's machine. `BLOCKED: <need>` if stuck.
