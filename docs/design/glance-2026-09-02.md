# The glance re-run — prd-33 wave 0 · 2026-09-02

**Protocol:** `docs/demo.md` — prd-03 ruling 25's four checks (GLANCE · PATHOLOGY · SCENE · MODE), held to prd-04 ruling 1's layman bar.
**Tree:** `main` at `0851512`. **Disposition:** prd-33 ruling 14 (2026-09-02) — the instrument half below is the wave-0 artefact; the lay-viewer half **transfers to prd-30's first-glance acceptance** and is recorded here as transferred, not skipped. **Recorded deferral:** `docs/design/ui-2.0-decisions.md:363` — the gate was amended on a documented deferral, booked "before the era closes rather than before its first dispatch". This is that booking, not a recovery from a miss.

## Two halves, deliberately separated

The charter calls this *"an operator act with a real lay viewer — deliberately not automatable"*. So this table has two kinds of column:

- **Instrument** — what the fixtures actually put on screen, read off the DOM and captured on 2026-09-02. Filled in below. EXECUTED.
- **Viewer** — what a first-time layman says, in the time the protocol allows. **Transferred to prd-30's acceptance act** (prd-33 ruling 14). A screenshot reading by the person who staged the fixtures is not a glance result, and this document does not pretend otherwise; the columns stay empty here so that nobody reads an agent's capture as a viewer's answer.

Under ruling 14 a viewer FAIL found in prd-30's act becomes an affordance or an explanation in prd-30's vocabulary — the CUT remedy ruling 5 held is given up, and ruling 14 says so.

## Setup that was actually used

| | |
|---|---|
| Checks 1–3 | `npm run dev:web` (Vite, :5173) — fixtures are client-side, no server needed. **Harness artefacts, not defects:** `/api/stream` 404 (no SSE), "could not load sessions" (SPA fallback returns HTML for `/api/sessions`), `CONNECTION ERROR` badge on `live`. |
| Check 4 | `npm start` (:4321, `tsx` source since `server/dist` is absent) watching this repo — 8 worktrees, 38 branches, recording to `session-1788326836561.jsonl`. Replayed **`session-1786415166167.jsonl` (2026-08-11, 1425 min)** from the recordings picker, at 16x, stepped with `]`. |
| Viewport | 1440×900 emulated for captures; scaled to 800×500 in the pane. |
| Tracked screenshots | **Not reused.** `docs/screenshots/fixture-*.png` are from `3bd1c5a` (2026-08-03); **84 scene commits** have landed since — growth, ambient, fruiting, light theme. New captures for the record must go through `scripts/dev/visit.mjs --capture` so they carry a manifest; the pane's screenshots do not and are not tracked. |

## Check 1 — GLANCE (fixture `2`, 20 lanes)

| Question | Instrument shows | Viewer answered (3 s) | Within 3 s? |
|---|---|---|---|
| Anything need me? | Pill `● ALL CLEAR`, evidence line `20 lanes · 20 branches · 20 files checked · collisions 0` | | |
| How many lanes working? | `20 lanes` on the strip; fleet header `20 lanes · all live` | | |
| Rough cost? | Burn `482.6K OUT`, `$277.57`, rate `$429.47/hr` | | |

**Instrument notes.** All three answers are on the strip; nothing requires hover, scroll or `v`. Dock caption states dollars are notional on subscription plans and estimates are flagged `est.` — the protocol's own honesty condition.

**Instrument half:** complete. **Viewer half:** carried by prd-30's acceptance.

## Check 2 — PATHOLOGY (fixture `3`, 9 lanes)

Strip reads **`● 5 NEED ATTENTION`**. Chips visible: `42-otel-receiver · no events for 11m03s` · `41-retry-parser · Bash→Read→Edit ×6, no commit` · `43-drawer-attach · workmux reports waiting 2m44s` · `45-ledger-subrows · outside fence — …spend-subrows.ts → 46-spend-selectors` · **`+1`** (collapsed).

| Pathology | Lane | Strip evidence | List STATE (after `v`) | Viewer pointed at it | Seconds |
|---|---|---|---|---|---|
| LOOPING | `41-retry-parser` | `Bash→Read→Edit ×6, no commit` | `LOOPING`, loop glyph, amber | | |
| FROZEN | `42-otel-receiver` | `no events for 11m03s` | `FROZEN`, severed bar, red | | |
| WAITING | `43-drawer-attach` | `workmux reports waiting 2m44s` | `WAITING`, raised hand, amber | | |
| EXPENSIVE | `44-scene-pulses` | behind `+1`; scene: white-hot thread, `126K out` | `EXPENSIVE`, ice | | |
| OFF-FENCE | `45-ledger-subrows` | `outside fence → 46-spend-selectors` (named victim) | `OFF-FENCE`, dashed arc to victim, amber | | |

**Instrument notes.**
- Count is five ✓. STATE column alone names all five with distinct glyph **and** hue ✓ (step 3's bar).
- **The fifth chip is collapsed into `+1`** at 1440 px. EXPENSIVE is the one hidden. Protocol says "five chips". Whether a viewer finds the fifth without `v` is a Viewer question — record it.
- **The right-hand LANES roster is not the STATE column.** It shows the *activity* word: `42-otel-receiver → IDLE`, and `41 / 44 / 45 → WORKING`; only `43 → WAITING`. A viewer who reads the roster instead of pressing `v` sees a frozen lane called idle and a looping lane called working. Not a protocol failure (the protocol names the list), but the first surface a glance lands on to the right of the organism. **Observation for the operator.**
- Tab title says `● 4 need you` while the strip says `5`. By design — `useTabSignal.ts:60` counts *summonses*; EXPENSIVE is not one, and `FleetContext.tsx:14` records that exact tension. Out of frame for a glance; noted.
- `ERRORS 2 ERR` in red on this fixture. Rendered from `burn.errorCount` (`panels/burn/format.ts:120`). **Fixture-side origin not traced** — `packages/core/src/fleet/fixtures.ts` has no explicit `collector.error` in the pathology spec, and its `:684` is inside `laneEndTs` (where a frozen lane's log stops), not error synthesis. Whether a viewer reads "2 errors" as a sixth pathology is a Viewer question; where the two come from is a small code question for whoever files it.

**Instrument half:** complete. **Viewer half:** carried by prd-30's acceptance.

## Check 3 — SCENE (fixture `3`, organism only, 30 s silent)

| Encoding the viewer must volunteer | Instrument renders it | Viewer said |
|---|---|---|
| Threads from a central mass are lanes/agents | ✓ nine threads from `MAIN` | |
| Thicker thread = produced more (not "working harder") | ✓ `44-scene-pulses` visibly widest (126K) | |
| Distance from mass = how far along | ✓ | |
| Brightness / travelling pulses = activity | ✓ | |
| Green working · amber waiting · hollow red dead | ✓ `42` red hollow arc; `43`/`45`/`41` amber | |
| Cut thread (narrows to nothing) = FROZEN | ✓ `42-otel-receiver` | |
| Needle tip = EXPENSIVE | ✓ `44-scene-pulses` | |
| Centre reads as one organic blob, not rings | ✓ soft contour, no facets | |

**Also captured, prd-33's own artefacts:**
- **Ruling 12 still** — `PAUSE MOTION` → button flips to `RESUME MOTION` with a `MOTION PAUSED` label beside it, in words. WCAG 2.2.2 path present and self-describing.
- **Light theme** (`prefers-color-scheme: light`) — mass renders as an ink wash on paper; frozen-red, waiting-amber and off-fence-amber-dashed all survive the carrier switch. Law 9a holds across themes.
- **Deterministic wander** — same bends on reload of `2` (prd-07 ruling 4). Not re-verified byte-for-byte; visually consistent across three loads.

**Instrument half:** complete. **Viewer half:** carried by prd-30's acceptance — this check's encoding list is the core of what that act asks.

## Check 4 — MODE (real server, recorded session, mid-scrub)

| Cue | Instrument shows |
|---|---|
| Banner replaces attention strip | ✓ `REPLAY · viewing a recorded past — not the live fleet` — strip **absent**, not tinted (ruling 16, `Shell.tsx:154`) |
| Timestamp + session identity | ✓ `2026-08-11 02:28:53 · 2:47 / 1425:38 · rhizomorph · session-1786415166167.jsonl` |
| Scrubber / transport | ✓ `Play · 1x 4x 16x · Return to live`; `]` steps chapters |
| Register | ice, not a ladder hue (`Banner.tsx:9`) |
| Elsewhere | LAB tab: `UNAVAILABLE DURING REPLAY — THE LAB FORKS LIVE CHECKPOINTS, AND THIS SESSION IS HISTORY` |

**Viewer said, unprompted:** ______________________ ☐ "this is the past" (or equivalent) ☐ asked "is this live?"

**Instrument notes.** Two `401 Unauthorized` in the console on replay entry on the real server — gated reads (prd-29). Replay loaded and advanced regardless; the `sse` badge sits in the banner. Not visible to a viewer; recorded for whoever owns prd-29's read policy.

**Instrument half:** complete. **Viewer half:** carried by prd-30's acceptance.

## Observations outside the four checks

These are not FAILs. They are things the run surfaced that the operator may want to file, and the protocol's own "what failure looks like" sections are the reference for each.

1. **`docs/demo.md` contradicts the code on `Esc`.** The Setup section says Esc "exits panel focus if a panel is focused". `app/PanelGrid.tsx:240`: *"`Escape` is not handled and must not be."* EXECUTED: after clicking `PAUSE MOTION` (inside the scene), `Esc` then `1` zoomed-to-fit instead of switching source — the exact inverse of the camera section's named failure. There is no keyboard way to hand focus back from the scene; only a click elsewhere. Either the doc or the shell is wrong about Esc — small, and it belongs to whoever owns `demo.md`. **Filed: #225.**
2. **The tracked fixture screenshots are 84 scene commits stale** (see Setup). prd-43 #22 binds a tracked PNG to its manifest; nothing binds it to the tree it depicts. A re-capture through `visit.mjs --capture` on this tree is the honest fix; a law that fails a fixture PNG older than N scene commits is the durable one. **Filed: #226, sequenced after #22.**
3. **The roster/STATE split** (Check 2 notes) — worth one lay viewer's reaction before deciding it is anything.

## What this run did not do

- No lay viewer. Every Viewer column is empty by design and by ruling 14 belongs to prd-30's act.
- No tracked screenshots produced — the pane's captures carry no manifest and would fail `no-personal-paths-law`.
- prd-33 closes on this table under ruling 14, with the viewer half transferred rather than declared passed.
